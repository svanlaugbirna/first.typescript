"use strict";
function square(num) {
    return num * num;
}
function greet(person) {
    person;
    return 'Hi friend, &{person}!';
    const doSomething = (person, age, isFunngy) => { };
}
square(3);
greet('Svanlaug');
